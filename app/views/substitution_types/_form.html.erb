<%= form_for(@substitution_type) do |f| %>
  <% if @substitution_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@substitution_type.errors.count, "error") %> prohibited this substitution_type from being saved:</h2>

      <ul>
      <% @substitution_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
